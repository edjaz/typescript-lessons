// Void

// Cela permet de définir une fonction de ne renvoie rien

function warnUser(): void {
    console.log("This is my warning message");
  }
 
  // on peut aussi définir une variable de ce type mais cela ne sert à rien car on ne peut pas l'utiliser
 let unusable: void = undefined;

  
  